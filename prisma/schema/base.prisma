// Prisma schema for MongoDB
// Connection URL is configured in prisma.config.ts (CLI) and PrismaClient constructor (runtime)

generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
